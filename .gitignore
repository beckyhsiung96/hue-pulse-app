# --- STRICT WHITELIST MODE ---
# 1. Ignore everything by default
*

# 2. Allow directories (needed to unignore files inside them)
!output_slices/
!.streamlit/

# 3. Allow specific root files
!pulse_app.py
!requirements.txt
!.gitignore

# 4. Allow content inside whitelisted directories
!output_slices/**
!.streamlit/config.toml

# 5. Explicitly IGNORE secrets and junk (overrides whitelist if needed)
.streamlit/secrets.toml
__pycache__/
*.pyc
.DS_Store
.env
venv/
env/